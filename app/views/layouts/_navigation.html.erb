<nav class="navbar navbar-expand-lg navbar-light bg-strong">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"> </a>
    <%= link_to 'BERETOR', root_path, class: "nav-link bg-strong" %> 
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <li class="nav-item">
          <%= link_to "Bloggers", users_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <% if logged_in? %>
          <%= link_to "Create new article", new_article_path, class: "dropdown-item" %>
          <% end %>
          </li>
          <li class="nav-item">
          <%= link_to "View articles", articles_path, class: "dropdown-item" %>
          </li>
        </li>
        <% if logged_in? %>

       
          <li class="nav-item">
            <%= "(Admin)" if current_user.admin? %>
            <%= link_to "View your profile", user_path(current_user), class: "dropdown-item" %>
            </li>
            <li class="nav-item">
            <%= link_to "Edit your profiles", edit_user_path(current_user), class: "dropdown-item" %>
            </li>
            <li class="nav-item">
            <%= button_to "Delete your profile", user_path(current_user), class: "dropdown-item text-danger", method: :delete, data: {confirm: "are you sure?"} %>

          </li>
          <li class="nav-item">
            <%= button_to 'Log out', logout_path, class: "nav-link", method: :delete, data: {turbolinks: false, confirm: "Are you sure you want to reset the indexes?" } %> 
          </li>
        <% else %>
        <li class="nav-item">
          <%= link_to 'Log in', login_path, class: "nav-link" %> 
        </li>
        <li class="nav-item">
          <%= link_to 'Sign up', signup_path, class: "nav-link" %> 
        </li>
        <% end %>
        
      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>